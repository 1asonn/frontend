import { AtmosphereParticle } from "@/THREE/atmosphere";


const AtmosphereRange = 1500
function IndexPage(){

    const TurnBasicNum = { firefly: }
    const Atomsphere1 = new AtmosphereParticle({
        longestDistance: AtmosphereRange,
        particleSum: 500,
        renderUpdate: (Point) => {
            // Point.rotation.x -= THREE.
        }
    })
}